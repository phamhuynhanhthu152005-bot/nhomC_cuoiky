<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <!-- Menu cha -->
    <menuitem
        id="learning_root_menu"
        name="Learning"
        sequence="1"/>

    <!-- Job Search -->
    <menuitem
        id="menu_skill"
        name="Job Search"
        parent="learning_root_menu"
        action="skill_action"
        sequence="1"/>

    <!-- Gợi ý khóa học -->
    <menuitem
        id="menu_course"
        name="Recommended Courses"
        parent="learning_root_menu"
        action="course_action"
        sequence="2"/>

    <!-- Kế hoạch đào tạo -->
    <menuitem
        id="menu_training"
        name="Training Plan"
        parent="learning_root_menu"
        action="training_action"
        sequence="3"/>

</odoo>